import { makeStyles } from '@material-ui/core/styles';

export const reposStyles = makeStyles((theme) => ({
    search: {
        marginTop: '10vh'
    },
    content: {
        margin: '0 50px'
    }
}));